# Seven-Gate Submission Status

This file maps the seven requested top-conference goals to authoritative
evidence. It is deliberately stricter than the repository's automated audits.
A script may verify hashes, counts, or formatting, but it cannot establish
novelty, importance, proof correctness, or the quality of an external review.

## Status Vocabulary

- `PASS`: every stated pass condition has authoritative evidence.
- `RUNNING`: the fixed protocol is being executed and no outcome has been read.
- `PENDING`: required work or evidence does not yet exist.
- `HUMAN ONLY`: completion requires an identifiable human action and cannot be
  filled by Codex or an internal script.

## Gate 1: New Shift-Aware Theory

**Status: PENDING HUMAN REVIEW.** The draft proves a simultaneous supported-shift
envelope, post-certification profile choice, common-radius validity, prospective
evidence allocation including an additive multi-cell theorem, an
independent-cell lower bound, and unsupported-cell impossibility. The
implementation mapping and exact finite-case checks exist for the original
certificate. The additive and inverse programs now have a hash-locked
primal-dual/KKT numerical-optimality contract; code and tests remain pending.
The gate cannot pass until an independent human checks every proof step and the
final literature refresh finds no unaddressed theorem-level collision.

Authoritative evidence:

- `appendix_shift_robust_theory.tex`
- `INDEPENDENT_THEORY_REVIEW_FORM.md`
- `FINAL_LITERATURE_REFRESH_SPEC.md`
- `theory_code_mapping.json` and its audit output
- `THEORY_CODE_MAPPING_V2_SPEC.md` for the pending complete extension map
- `ADDITIVE_ALLOCATION_EXTENSION_INTERPRETATION_2.md` for the pending
  primal-dual and KKT implementation audit

## Gate 2: Theory Matched by Data

**Status: PENDING CONTROLLED CONFIRMATION.** The 54-cell exact study and 216-cell
extension are complete, including independent seeded replays. A separately
locked five-dataset diagnostic at 5%, 10%, 25%, 50%, and 100% certification size
placed all five observed curves inside their plug-in bootstrap bands. That
real-data overlay is descriptive because the full certification fold estimates
its bootstrap law; only the known-law study is an exact theory prediction. The
fresh controlled-study replay source and sealed pre-comparison execution are
now hash-specified; the false-acceptance checks still must be generated and
reported even if they disagree with theory. A frozen non-rescuing usefulness
sensitivity also retains bootstrap resamples with zero oracle-safe opportunities
instead of silently dropping them. A second replay interpretation requires exact
candidate/profile/allocation/decision cardinalities, unique logical keys, and
safe/violation/opportunity invariants before aggregation. Replay Interpretation
4 additionally requires 73,728 candidate-configuration records whose sufficient
statistics reconstruct the full coupled envelope rather than only its axis
intercepts.

Authoritative evidence:

- exact-study and family-grid reports under `research/artifacts/`
- `vera_learning_curve_diagnostic.json` and its locked figure
- controlled-study analysis outputs
- final overlay figure and full result table
- `CONTROLLED_SHIFT_USEFULNESS_SENSITIVITY_SPEC.md`

## Gate 3: Deployment-Rule Experiment

**Status: RUNNING, OUTCOME BLIND.** The locked 1,280-run matrix compares nine
matched deployment rules on the same candidate edits, including always deploy,
validation point selection, IID LTT, robust point selection, scalar robust
certification, VERA, common radius, and the exact shifted-law opportunity
oracle. Its four primary
gates are immutable. No secondary result may replace a failed primary gate.
Static pre-outcome review found an envelope-cap implementation mismatch (locked
analyzer 4 versus preregistered 8). Receipt generation is unaffected. Cap 8 is
frozen as protocol authority, cap 4 remains a required disclosed sensitivity,
and two independent cap-8 paths must agree before this gate can pass.
The independent path must also preserve full coupled-envelope curve statistics
and distinguish coordinate intercepts from common-radius limiting contracts.
A separately frozen supporting stress compares always deploy, validation point
selection, and VERA across three exact contract severities. It requires both
naive rules to reach 20% violations somewhere at the registered contract, VERA
to remain at or below 5% in every dataset--severity cell, and registered safety
and usefulness to pass. It cannot change a primary gate and must be reported as
a negative result if it fails.

Authoritative evidence after completion:

- `prereg_controlled_shift.json` and its SHA-256 sidecar
- 1,280 valid official-method receipts with zero proxy or missing rows
- exact agreement between the protocol-compliant cap-8 analyzer and the
  independent cap-8 raw-array replay
- unchanged cap-4 sensitivity output, three-way comparison report, and
  registered sensitivity analyses
- `THREE_RULE_THRESHOLD_STRESS_SPEC.md` and its complete 36-cell result

## Gate 4: Official Baselines and Cross-Modal Coverage

**Status: RUNNING.** The prior five-dataset studies have 1,000 audited official
method receipts across INLP, R-LACE, LEACE, TaCo, and MANCE++. The controlled
supported-shift matrix is still running on Waterbirds, CivilComments, Bios, and
GaitPDB; Camelyon17 remains the separate unseen-support impossibility study. This
gate passes only after all 1,280 new receipts pass the full provenance audit.

Authoritative evidence:

- official upstream commit inventory
- full controlled-study receipt audit
- `CONTROLLED_SHIFT_RECEIPT_AUDIT_INTERPRETATION_1.md`
- `CONTROLLED_SHIFT_RECEIPT_AUDIT_INTERPRETATION_2.md`
- `DATA_SOFTWARE_LICENSE_LEDGER.md`

## Gate 5: One Memorable Receipted Number

**Status: PENDING OUTCOME ACCESS.** The final abstract sentence must report naive
contract violation, VERA false acceptance, and retained certifiable opportunity
directly from audited receipts. If the controlled primary gates fail, the
abstract must lead with the theory and the failure or forced-abstention result;
it may not substitute a favorable secondary profile.
The three-rule sentence is additionally forbidden unless the frozen threshold-
stress supporting condition passes at the registered contract without a
supplementary-severity rescue.

Authoritative evidence:

- locked analyzer output and independent replay
- `CONTROLLED_SHIFT_REPORTING_SPEC.md`
- `THREE_RULE_THRESHOLD_STRESS_SPEC.md`
- final claim-ledger row with exact numerator, denominator, and interval

## Gate 6: Main-Track Presentation

**Status: PENDING FINAL RESULTS AND FORMAT CHECKS.** The manuscript is a full
draft with a dedicated related-work section, more than 40 active references,
one deterministic three-panel teaching figure included identically in all
three venue sources, one planned main result table, explicit constants, and
named and anonymous wrappers. It still requires result insertion, exact page
fit, clean compilation, visual inspection, archive anonymization, and official
venue-policy checks. The working AAAI sources are modular and still require the
official-kit-compliant single-TeX finalization gate. Outcome-blind ICLR and
NeurIPS scientific-content bodies now exist and pass neutral syntax builds, but
they still require shared controlled results, result-dependent supplement
sections, the result figure and table, review, and exact formatting after their official
2027 materials exist. The shared supplement core passes a neutral syntax build
and visual inspection; that is not an official-format pass. All three are bound
to the strict V2 result schema and one section-level claim map.

Authoritative evidence:

- named and anonymous manuscript sources and PDFs
- `CONTROLLED_SHIFT_REPORTING_SPEC.md`
- `AAAI_SOURCE_FINALIZATION_SPEC.md`
- `DOUBLE_BLIND_RELEASE_BOUNDARY.md`
- `DOUBLE_BLIND_RELEASE_BOUNDARY_INTERPRETATION_3.md` for the human-approved
  VERA license and third-party exclusion boundary
- `SHARED_RESULT_MANIFEST_SPEC.md` and
  `SHARED_RESULT_MANIFEST_SCHEMA.json`
- `THREE_VENUE_CONTENT_VARIANT_SPEC.md` and
  `VENUE_VARIANT_CLAIM_MAP.md`
- `FIGURE1_IMPLEMENTATION_AUDIT.md`
- final presentation, anonymity, reference, license, and clean-room audits
- `VENUE_FORMAT_PLAN.md`

## Gate 7: External Adversarial Review

**Status: HUMAN ONLY, 0/5 COMPLETE.** Four role-specific cold reviews and one
fresh post-revision review are required. Every reviewer must address overlap
with Learn Then Test and Prompt Risk Control. Internal hostile-review documents
prepare the paper but do not count as external review.

Authoritative evidence:

- completed `external_review_registry.json` entries from real reviewers
- `review_response_ledger.md` with every major finding fixed or rebutted
- a frozen post-revision paper hash reviewed by the fifth reader

## Overall Decision

**NOT SUBMISSION READY.** Gates 2--6 still require experiment-dependent work,
Gate 1 still requires independent proof and novelty review, and Gate 7 is a
human-only blocker. The overall state may change to `PASS` only when every gate
above passes with its listed evidence. Conference acceptance is never a
guaranteed or auditable project outcome.
