# MOSAIC LLM Container Plan

The V9 paper remains the immutable fallback. The Qwen section is included only
after a pilot-triggered, pre-outcome lock and complete confirmation run.

The main paper is already seven content pages. The new paragraph replaces, not
supplements, the current space budget:

1. Compress `Alphabet sensitivity` from twelve lines to five, retaining the
   K=64 scaling result and the two-of-three high-support K=8 result.
2. Merge the `Operational use` discussion into the conclusion, saving four to
   five lines.
3. Place detailed Qwen architecture, pilot exclusion, per-seed results, and
   representation ablations in the technical supplement.

The submitted abstract keeps its current order and wording. If the locked
confirmation supports an LLM release, add one result sentence immediately
before the geographic-confirmation sentence. Do not otherwise restructure the
abstract.

After integration, anonymous and named PDFs must be rebuilt from clean source.
The code/data archive must include the pilot boundary, confirmation lock,
receipts, library, and replay tests. The anonymity audit must rescan every
archive member and visible PDF text from scratch.
