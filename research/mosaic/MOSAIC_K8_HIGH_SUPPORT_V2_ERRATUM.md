# K=8 high-support v2 execution note

The v2 protocol completed its lock and began the first official candidate
family. It then stopped before a full 13-candidate frontier, certification
table, selection, or receipt was produced because the active Python 3.9
interpreter could not import the official LEACE package, which uses Python
3.10 union-type syntax.

The v3 protocol locks the existing Python 3.12 real-experiment environment
only after importing all five official adapter entry points. No v2 candidate
frontier outcome is used in the paper.
